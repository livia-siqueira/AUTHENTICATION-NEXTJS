import styles from './PageInit.module.css'

const PageInit = () => {
    return(
        <section className={styles.starting}>
            <h1>Welcome on Board!</h1>
        </section>
    )
}

export default PageInit;